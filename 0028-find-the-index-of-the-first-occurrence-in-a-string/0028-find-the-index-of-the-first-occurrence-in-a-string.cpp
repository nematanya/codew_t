class Solution {
public:
    int strStr(string h, string needle) {
        // int n=needle.size();
        // for(int i=0;i<h.size()-n;i++)
        // {
        //     if(h.substr)
        // }
        return h.find(needle);
    }
};